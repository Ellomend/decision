{
  "name": "dm",
  "version": "1.0.0",
  "scripts": {
    "apistart": "cd backend && npm install && npm run start:dev",
    "frontstart": "cd frontend && npm install && npx quasar dev",
    "start": "concurrently \"npm run apistart\" \"npm run frontstart\" "
  },
  "dependencies": {
    "concurrently": "^6.2.0"
  }
}
